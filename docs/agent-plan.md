# Agent Plan

## Goal

`{{one_sentence_goal}}`

## Suggested Use

Use before an agent makes non-trivial edits. The plan should be approved, revised, or rejected before implementation starts.

## Current Understanding

- Task: `{{task_summary}}`
- Repository area: `{{area}}`
- User-visible outcome: `{{outcome}}`
- Known constraints: `{{constraints}}`

## Assumptions

| Assumption | Evidence | Needs confirmation? |
| --- | --- | --- |
| `{{assumption}}` | `{{evidence}}` | `{{yes_no}}` |

## Files To Read

- `{{path}}`: `{{why_read}}`

## Files To Edit

- `{{path}}`: `{{expected_change}}`

## Files Not To Touch

- `{{path}}`: `{{reason}}`

## Steps

1. `{{step_1}}`
2. `{{step_2}}`
3. `{{step_3}}`

## Validation

| Check | Command or method | Expected result |
| --- | --- | --- |
| `{{check_name}}` | `{{command_or_method}}` | `{{expected_result}}` |

## Risk Review

- Main risk: `{{risk}}`
- Mitigation: `{{mitigation}}`
- Approval needed before action: `{{approval_needed}}`

## Stop Points

Stop before:

- `{{stop_point}}`

## Handoff Summary

If another agent or reviewer takes over:

```text
Goal: {{goal}}
Plan status: {{approved_rejected_pending}}
Next action: {{next_action}}
Open questions: {{open_questions}}
```
