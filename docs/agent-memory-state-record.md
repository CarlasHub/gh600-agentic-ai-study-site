# Agent Memory And State Record

## Goal

Keep durable state explicit across long-running, resumed, or multi-agent work.

## Suggested Use

Use when an agent session is paused, compacted, resumed, handed off, or expected to remember external facts.

## Current Goal

`{{current_goal}}`

## Completed Actions

- `{{completed_action}}`

## Active Assumptions

| Assumption | Evidence | Risk if wrong |
| --- | --- | --- |
| `{{assumption}}` | `{{evidence}}` | `{{risk}}` |

## External IDs And Artifacts

| Type | ID or link | Purpose |
| --- | --- | --- |
| `{{issue_pr_trace_doc}}` | `{{id_or_url}}` | `{{purpose}}` |

## Open Risks

- `{{open_risk}}`

## Decisions Made

| Decision | Owner | Evidence |
| --- | --- | --- |
| `{{decision}}` | `{{owner}}` | `{{evidence}}` |

## Next Concrete Action

`{{next_action}}`

## What Not To Forget

- `{{constraint_or_fact}}`

## Staleness Review

Review this record when:

- source documents change;
- issue scope changes;
- another agent takes over;
- validation fails;
- more than `{{number}}` days pass.
